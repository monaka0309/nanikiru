<%= link_to post_path(post) do %>
    <div class="post-card bg-white rounded p-5 my-4 sm:p-10">
        <div class="flex justify-left items-center gap-4">
            <div><%= image_tag post.user.image.url, class: 'aspect-square rounded-full ring-1 ring-gray-400 mb-3', size: '50x50' %></div>
            <div><%= post.user.name %></div>
        </div>
        <p class="text-gray-700 mb-3" style="font-size: 16px;"><%= post.content %></p>
        <% if post.image.present? %>
            <%= image_tag post.image.url, class: "rounded" %>
        <% end %>
<% end %>
    <% if logged_in? %>
        <div class="text-md font-mono my-4">下記の選択肢から投票してください。</div>
    <% else %>
        <div class="my-5"></div>
    <% end %>
    <div class="grid grid-cols-1">
        <div class="rounded-lg px-1">
            <div>
                <%= render 'posts/post_tiles', post: post %>
            </div>
        </div>
    </div>
    <p class="text-slate-400 text-sm text-right mt-2">
        <%= post.created_at.strftime('%Y/%m/%d %H:%M') %>
    </p>
    <li class="fa-solid fa-comment-dots sm:fa-lg">
        <%= link_to "コメント数：", post_path(post) %>
        <%= post.comments.count %>
        <%= link_to "https://twitter.com/share?url=https://www.nanikiru-mahjong.com/posts/#{post.id}&text=あなただったら何切る？", target: '_blank' do %>
            <%= image_tag("x.jpg", class: 'search-icon h-9 p-1 rounded-full bg-black mt-2') %>
        <% end %>
    </li>
</div>
